#include <stdio.h>

void stringconcatenate(char* source,char* target){
    //target jadi source
    *source += 


}

int main(){
    char a[100] = "Day";
    char b[100] = "Giovanni";
    stringconcatenate(a,b);
    printf("a:%s",a);
    return 0;
}